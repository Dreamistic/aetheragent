# VAEAGENT 启动说明

这份文档用于记录本机 Windows 11 上如何启动 VAEAGENT，以及系统提示词放在哪里。

## 1. 系统提示词位置

系统提示词在后端目录：

- 简体中文：`backend/app/prompts/zh-Hans/base.md`
- 繁体中文：`backend/app/prompts/zh-Hant/base.md`
- 英文：`backend/app/prompts/en/base.md`

提示词组装逻辑在：

- `backend/app/services/prompts.py`

Skills 会自动注入系统提示词，位置是：

- `backend/skills/<skill-id>/SKILL.md`
- 当前已有：`backend/skills/tool-workflow/SKILL.md`、`backend/skills/mcp/SKILL.md`

路由判断逻辑在：

- `backend/app/services/routing.py`

修改提示词后，建议重启后端服务。

## 2. 后端启动

在 PowerShell 中进入项目根目录：

```powershell
cd path\to\VAEAGENT
```

如果还没有安装依赖：

```powershell
pip install -r requirements.txt
```

如果要使用真实模型，先设置 OpenRouter Key：

```powershell
$env:OPENROUTER_API_KEY="你的 OpenRouter Key"
```

启动后端：

```powershell
.\scripts\run-backend.ps1
```

后端地址：

- API：`http://127.0.0.1:8000/api`
- 网页端：`http://127.0.0.1:8000/`

如果没有设置 `OPENROUTER_API_KEY`，后端会使用本地回退回复，方便测试登录、会话、UI 和流式显示。

## 3. 桌面端启动

Flutter 已安装在：

```text
C:\src\flutter
```

开发模式运行 Windows 客户端：

```powershell
cd path\to\VAEAGENT\client
C:\src\flutter\bin\flutter.bat run -d windows
```

已构建好的 release 程序位置：

```text
path\to\VAEAGENT\client\build\windows\x64\runner\Release\vaeagent_client.exe
```

## 4. 网页端启动

如果前端代码修改过，先重新构建 Web：

```powershell
cd path\to\VAEAGENT
.\scripts\build-web.ps1
```

然后启动后端：

```powershell
.\scripts\run-backend.ps1
```

打开：

```text
http://127.0.0.1:8000/
```

FastAPI 会自动把 `client/build/web` 作为网页端服务出来，API 仍然在 `/api` 下。

如果网页端更新后仍显示旧界面，打开一次缓存清理页：

```text
https://your-domain.example.com/reset.html
```

它会注销旧的 Flutter service worker、清理浏览器缓存，然后自动回到聊天页。

## 5. Cloudflare 域名访问

当前域名：

```text
https://your-domain.example.com/
```

Cloudflare Tunnel 配置文件：

```text
path\to\VAEAGENT\cloudflared-config.yml
```

公开仓库里只提交 `cloudflared-config.example.yml`。把它复制为 `cloudflared-config.yml` 后，再填入你自己的 tunnel ID、credentials path 和 hostname；本地的 `cloudflared-config.yml` 已加入 `.gitignore`。

启动 tunnel：

```powershell
cd path\to\VAEAGENT
.\run-tunnel.cmd
```

检查 tunnel 是否正在运行：

```powershell
Get-Process cloudflared -ErrorAction SilentlyContinue
```

如果需要重启 tunnel：

```powershell
Get-Process cloudflared -ErrorAction SilentlyContinue | Stop-Process -Force
.\run-tunnel.cmd
```

如果 Cloudflare 控制台里启用了 Access 保护，访问域名时会先进入 Cloudflare 登录页。要公开访问，需要在 Cloudflare Zero Trust / Access 中调整对应应用策略。

## 6. 模型配置

默认模型配置在：

```text
config/app.yaml
```

当前默认：

```yaml
agent:
  provider: openrouter
  base_url: https://openrouter.ai/api/v1
  model: z-ai/glm-5.2
  openrouter_provider: novita/fp8
```

环境变量示例在：

```text
.env.example
```

常用环境变量：

```powershell
$env:OPENROUTER_API_KEY="你的 OpenRouter Key"
$env:OPENROUTER_PROVIDER="novita/fp8"
$env:OPENAI_MODEL="z-ai/glm-5.2"
```

## 7. 工具调用、MCP 和 Skills

工具调用链路：

- 原生 OpenAI/OpenRouter `tool_calls` 会直接执行。
- 如果模型输出旧 Agent 兼容的 `<function_calls>...</function_calls>`，后端也会解析并执行。
- 工具调用、工具结果、需要补充信息、需要确认，都会作为 `function` 消息写入会话，刷新页面后仍可看到。
- 每轮对话第一条流事件会带 `trace_id`，可用于查日志。

MCP 配置：

登录后打开设置，找到 `MCP Server` 区域。

添加 MCP Server 时常用字段：

- `名称`：自己容易识别的名字。
- `传输方式`：可选 `Streamable HTTP`、`SSE`、`stdio`。
- `MCP HTTP/SSE 地址`：例如 `http://127.0.0.1:9000/mcp`。
- `请求头 JSON`：如果 MCP 服务需要鉴权，可以填，例如：

```json
{
  "Authorization": "Bearer your-token"
}
```

添加后可以点测试按钮读取该 MCP Server 暴露的工具。聊天时，启用的 MCP 工具会自动暴露给模型调用。

stdio MCP 示例：

- `MCP stdio 命令`：`npx`
- `MCP stdio 参数 JSON`：

```json
["-y", "@modelcontextprotocol/server-filesystem", "path\\to\\VAEAGENT"]
```

注意：stdio MCP 会启动本机命令，只添加你信任的 MCP Server。

Skills 配置：

- 打开设置可以看到已加载 Skills。
- 本地新增 skill 时，新建目录 `backend/skills/你的skill名/SKILL.md`，重启后端后生效。

## 8. 日志排查

日志目录：

```text
path\to\VAEAGENT\logs
```

主要文件：

- `logs/vaeagent.log`：人类可读运行日志。
- `logs/events.jsonl`：结构化事件日志，包含 chat、tool、mcp 等事件。

也可以登录后访问接口查看最近事件：

```text
http://127.0.0.1:8000/api/logs/events?limit=200
```

排查工具调用时，优先看同一个 `trace_id` 下的：

- `chat.turn_start`
- `chat.llm_stream_done`
- `tool.call_start`
- `tool.call_done`
- `mcp.tools_list_*`
- `mcp.tool_call_*`

## 9. 字体配置

当前已接入霞鹜文楷字体，字体文件位置：

```text
client/assets/fonts/lxgw_wenkai/
```

Flutter 字体注册位置：

```text
client/pubspec.yaml
```

字体角色集中配置位置：

```text
client/lib/src/core/app_fonts.dart
```

当前预留了这些字体角色：

- `global`：全局默认字体。
- `ui`：界面控件字体。
- `message`：聊天消息字体。
- `heading`：标题字体。
- `code`：代码块字体。

如果以后只想换某个区域的字体，优先修改 `app_fonts.dart` 里的对应字段。

## 10. 常用测试命令

后端测试：

```powershell
cd path\to\VAEAGENT
python -m pytest -q backend\tests
```

Flutter 检查：

```powershell
cd path\to\VAEAGENT\client
C:\src\flutter\bin\flutter.bat analyze
C:\src\flutter\bin\flutter.bat test
```

构建桌面端：

```powershell
cd path\to\VAEAGENT\client
C:\src\flutter\bin\flutter.bat build windows --release
```

构建网页端：

```powershell
cd path\to\VAEAGENT
.\scripts\build-web.ps1
```
